import BookButton from '@/components/BookButton';
import Logo from '@/components/Logo';
import { whatsappGeneral } from '@/lib/contact-links';

/**
 * Шапка сайта: знак, ведущий на главную, и единственное действие — запись.
 *
 * ЗАЧЕМ ВООБЩЕ. До неё с экрана теста не было пути назад: ученик, дошедший до
 * разбора, не мог вернуться и посмотреть, кто это всё написал и что ещё есть.
 * Это дыра в навигации, а не украшение.
 *
 * ПОЧЕМУ НЕ ПРИЛИПАЕТ К ВЕРХУ. Просили кнопку «везде и всегда», но на экране
 * теста уже есть прилипшая полоса прогресса сверху и панель ответов снизу.
 * Третий закреплённый элемент на телефоне съедает ту высоту, где должен быть
 * сам вопрос со схемой. Важнее другое: воронка здесь «тест → результат →
 * разговор», и кнопка записи посреди теста уводит человека ровно до того
 * момента, ради которого он и пришёл. Результат — единственное, что делает
 * разговор предметным: преподавателю есть что сказать с первой фразы.
 * Поэтому шапка обычная: на главной она видна сразу, а на тесте уезжает
 * вверх и не мешает.
 */
export default function SiteHeader() {
  return (
    <header className="border-b border-rule">
      <div className="mx-auto flex max-w-content items-center justify-between gap-4 px-5 py-3 sm:px-8">
        <Logo />
        <BookButton href={whatsappGeneral()} size="sm" label="Free consultation" />
      </div>
    </header>
  );
}
