import { TEACHER } from '@/config/teacher';

/**
 * Основной адрес сайта — тот, который должен стоять в поиске.
 *
 * Отсюда его берут robots.txt и sitemap.xml, а дальше будут брать canonical
 * и разметка JSON-LD. Адрес в одном месте, потому что расхождение между ними
 * хуже, чем ошибка в каждом по отдельности: поисковик получает два разных
 * ответа на вопрос «какой адрес у этой страницы» и выбирает сам.
 *
 * Если у Ольги появится свой домен — поменять здесь и настроить в Vercel
 * постоянный (301) редирект со всех адресов vercel.app на новый.
 * Без слеша в конце.
 */
export const SITE_URL = 'https://olga-shalamai.vercel.app';

/**
 * Как сайт подписан в поиске: заголовок и описание главной.
 *
 * Тексты из плана продвижения (§5). Лежат здесь, а не в самой странице,
 * потому что у них два читателя: главная ставит их себе, а layout — запасными
 * для страниц без собственных метаданных (сейчас это 404). Две копии одной
 * строки разъезжаются молча, и заметить это можно только в выдаче Google.
 *
 * Знак ® добавлен к AP: он обязателен при каждом упоминании (принцип 4),
 * в том числе в заголовке вкладки и в описании — их видно в выдаче.
 * Имя берётся из TEACHER, чтобы решение про «Dr.» (TODO.md, раздел 0)
 * менялось в одном месте, а не расходилось с остальным сайтом.
 *
 * ЗАГОЛОВОК СОБРАН ИЗ СЛОВ ПЛАНА, НО В ДРУГОМ ПОРЯДКЕ. В плане он записан
 * как «Olganomics | AP Economics and IGCSE Business and Economics Support» —
 * это 677px, а Google обрезает заголовок примерно на 600px (Arial 20px) и
 * съел бы «IGCSE Economics» вместе с хвостом. Нынешние 569px влезают целиком.
 * Порядок тоже не косметика: первыми читаются слова, которые люди набирают
 * в поиске, а бренд «Olganomics» пока не ищет никто — ему место в конце,
 * где он работает подписью, а не тратит начало строки. Длину новой версии
 * проверять так же: Arial 20px, лимит ~600px (на телефоне ~580px).
 */
export const SITE_TITLE = 'AP® Economics, IGCSE Economics and Business | Olganomics';

export const SITE_DESCRIPTION =
  `Exam-focused AP® Economics, IGCSE Economics and IGCSE Business support with ${TEACHER.name}. ` +
  'Take a free diagnostic or book a consultation.';
